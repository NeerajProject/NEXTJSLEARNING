'use client';
import React from 'react'
export const ProductCard = () => {
  return (
    <div>ProductCard
            <button onClick={() => alert('Button clicked!')}>Click</button>

    </div>
  )
}
